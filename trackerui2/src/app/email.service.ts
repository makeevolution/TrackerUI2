import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})  /* this service is generated automatically using ng g s email. Injectable is so that if this EmailService has an argument, that argument will be automatically provided by the DI framework */
export class EmailService {

  constructor() { }
}
